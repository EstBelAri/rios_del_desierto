from django.urls import path
# from .views import GestorEstudios, Estudios, ListaEstudios, InformacionEstudio, ModalidadesEstudio, Modalidades, GetTenPercent

urlpatterns = [
    # path('gestor_estudios/', GestorEstudios.as_view(), name='gestor_estudios'),
    # path('ten_percent/', GetTenPercent.as_view(), name='ten_percent'),
    # path('estudios/', Estudios.as_view(), name='estudios'),
    # path('modalidades/', Modalidades.as_view(), name='modalidades'),
    # path('lista_estudios/', ListaEstudios.as_view(), name='lista_estudios'),
    # path('informacion_estudio/nombre_integra=<str:nombre_integra>/',
    #      InformacionEstudio.as_view(), name='informacion_estudio'),
    # path('modalidades_estudio/nombre_integra=<str:nombre_integra>/',
    #      ModalidadesEstudio.as_view(), name='modalidades_estudio')
]